import { Router } from "express";
import { } from "../controllers/productsController";

const router = Router()

router.get('',)
router.post('',)
router.get('/:id',)
router.put('/:id',)
router.delete('/:id',)

export default router